@if (ViewBag.LoggedUserId > 0)
{
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}
@if (ViewBag.LoggedUserId < 0)
{
    Layout = "~/Views/Shared/_UnloggedLayout.cshtml";
}

@model IEnumerable<LibraryWebApplication1.Models.Specialty>

@{
    ViewData["Title"] = "Index";
}

<h1>Specialties</h1>

@if (ViewBag.LoggedUserId == 0)
{
    <p>
        <a asp-action="Create" class="btn btn-warning">Add new specialty</a>
    </p>
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SpecialtyName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SpecialtyName)
            </td>
            <td>
                    @if (ViewBag.LoggedUserId == 0)
                    {
                        <a asp-action="Edit" asp-route-id="@item.SpecialtyId" class="btn btn-info">Edit</a>
                        <a asp-action="Delete" asp-route-id="@item.SpecialtyId" class="btn btn-danger">Delete</a>
                    }
            </td>
        </tr>
}
    </tbody>
</table>
